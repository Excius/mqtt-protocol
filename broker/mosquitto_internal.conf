# Internal broker configuration - used behind the security proxy
# No TLS (proxy handles TLS termination), localhost only
# This broker should NEVER be exposed to external networks

listener 1884 127.0.0.1
allow_anonymous true
